# Self-hosted Voice-to-Text (Whisper) dependencies
#
# === IMPORTANT FOR CONDA / CORPORATE ENVIRONMENTS ===
#
# - faster-whisper pulls in torch + ctranslate2 (this is the biggest part of the size).
# - For sounddevice on conda: conda install python-sounddevice -c conda-forge
#
# To control CUDA version (critical when you have driver limitations):
#   conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia
#   pip install faster-whisper
#
# Minimal core:
#   pip install faster-whisper numpy

faster-whisper>=1.0.0
numpy>=1.24

# Optional
sounddevice>=0.4.6

# === ALTERNATIVE (simpler reference) ===
# openai-whisper
# (use only one of faster-whisper or openai-whisper)

# Note: scipy is no longer a dependency.