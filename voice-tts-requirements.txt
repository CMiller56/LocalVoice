# Text-to-Speech (TTS) dependencies for LocalVoice
#
# === IMPORTANT NOTES FOR CONDA / LARGE CORPORATE ENVIRONMENTS ===
#
# - piper-tts has no conda package. You must: pip install piper-tts
# - For sounddevice on conda, prefer:
#     conda install python-sounddevice -c conda-forge
#
# - There is **no auto-download** in this version.
#   You must manually download voices from:
#   https://huggingface.co/rhasspy/piper-voices
#
# Recommended minimal install:
#   pip install piper-tts numpy
#
# With optional playback:
#   pip install sounddevice
#   # or use conda for sounddevice as noted above

piper-tts>=1.2.0
numpy>=1.24

# Optional (only for .speak() convenience method)
sounddevice>=0.4.6